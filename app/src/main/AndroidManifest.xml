<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.motion">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sample.CollapsingToolbarActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sample.PropertyAnimatorActivity" />
        <activity android:name=".sample.ViewAnimationActivity" />
        <activity android:name=".sample.AutoAnimateLayoutActivity" />
        <activity android:name=".sample.AnimateBetweenActivity" />
        <activity android:name=".sample.AnimateBetweenActivity2" />
        <activity android:name=".sample.LayoutTransitionActivity" />
        <activity android:name=".sample.MotionLayoutActivity" />
        <activity android:name=".sample.MotionLayoutEasyActivity" />
        <activity android:name=".sample.MotionLayoutCustomViewActivity" />
        <activity android:name=".sample.CoordinatorWithPropertyActivity" />
        <activity android:name=".sample.MotionLayoutPathActivity" />
    </application>

</manifest>